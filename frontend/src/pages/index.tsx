import { AuthCard } from "@/components/authCard";
import { Header } from "@/components/Header";
import React from "react";

const Main = () => {
  return (
    <div>
      {/* <Header /> */}
      <AuthCard />
    </div>
  );
};

export default Main;
